{% extends ':Admin:dashboard.html.twig' %}

{% block title %}Liste des types de cuisson - {{ parent() }}{% endblock %}

{% block content %}
    <div id="cookingType" class="item">
        <div class="flex-center">
            <div class="col-12 col-sm-10 col-md-8 col-lg-6 mb-r">
                <div class="card card-cascade">
                    <div class="view gradient-card-header red darken-3">
                        <h2 class="h2-responsive">Types de cuisson</h2>
                        <p>Gestion des types de cuisson</p>
                    </div>
                    <div class="card-body text-center">
                        <div class="card-text">
                            {% if cookingType is not empty %}
                                <table class="table table-striped">
                                    <tbody>
                                    {% for type in cookingType %}
                                        <tr class="row">
                                            <td class="card-text col-7">{{ type.name }}</td>
                                            <td class="text-center col-5">
                                                <a href="{{ path('admin_cooking_type_update', {'id': type.id}) }}"
                                                   class="teal-text"><i class="fa fa-pencil fa-lg"></i></a>
                                                <a href="{{ path('admin_cooking_type_delete', {'id': type.id}) }}"
                                                   class="red-text"><i class="fa fa-times fa-lg"></i></a>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                                <div class="navigation flex-center">
                                    {{ knp_pagination_render(cookingType) }}
                                </div>
                            {% else %}
                                <p>Aucun type de cuisson.</p>
                            {% endif %}
                        </div>
                        <hr>
                        <a class="btn red darken-3" href="{{ path('admin_cooking_type_add') }}"><i
                                    class="fa fa-check"></i> Ajouter un type de cuisson</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascript %}

{% endblock %}