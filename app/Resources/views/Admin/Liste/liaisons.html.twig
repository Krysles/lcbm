{% extends ':Admin:dashboard.html.twig' %}

{% block title %}Liste des liaisons - {{ parent() }}{% endblock %}

{% block content %}
    <div id="liaison" class="item">
        <div class="row flex-center">
            <div class="col-12 col-sm-10 col-md-8 col-lg-6 mb-r">
                <div class="card card-cascade">
                    <div class="view gradient-card-header red darken-3">
                        <h2 class="h2-responsive">Liaisons</h2>
                        <p>Gestion des liaisons</p>
                    </div>
                    <div class="card-body text-center">
                        <div class="card-text">
                            {% if liaisons is not empty %}
                                <table class="table table-striped">
                                    <tbody>
                                    {% for liaison in liaisons %}
                                        <tr class="row">
                                            <td class="card-text col-7">{{ liaison.name }}</td>
                                            <td class="text-center col-5">
                                                <a href="{{ path('admin_liaison_update', {'id': liaison.id}) }}"
                                                   class="teal-text"><i class="fa fa-pencil fa-lg"></i></a>
                                                <a href="{{ path('admin_liaison_delete', {'id': liaison.id}) }}"
                                                   class="red-text"><i class="fa fa-times fa-lg"></i></a>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                                <div class="navigation flex-center">
                                    {{ knp_pagination_render(liaisons) }}
                                </div>
                            {% else %}
                                <p>Aucune liaison.</p>
                            {% endif %}
                        </div>
                        <hr>
                        <a class="btn red darken-3" href="{{ path('admin_liaison_add') }}"><i
                                    class="fa fa-check"></i> Ajouter une liaison</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascript %}

{% endblock %}